VITE_FIREBASE_API_KEY="AIzaSyBDyo-7CGen47Udcc-0X7NYhkArs6lPf48"
VITE_FIREBASE_AUTH_DOMAIN="pulsemaint.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="pulsemaint"
VITE_FIREBASE_STORAGE_BUCKET="pulsemaint.firebasestorage.app"
VITE_FIREBASE_MESSAGING_SENDER_ID="451898279733"
VITE_FIREBASE_APP_ID="1:451898279733:web:aee2db5a78e05d8e452e7c"
# App Configuration
VITE_APP_URL="https://pulsemaint.web.app"

# Google OAuth — required for "Push to Google Sheets" in the Reports module.
# Create an OAuth 2.0 Client ID (type: Web application) in Google Cloud Console,
# enable the Google Sheets API, and add your app origin (e.g. the value of
# VITE_APP_URL and http://localhost:5173 for dev) to the "Authorized JavaScript
# origins" and "Authorized redirect URIs". Required scopes are requested at
# runtime: spreadsheets + drive.file. Leave blank to disable Sheets push (the
# Reports "Sheets" button then falls back to a CSV download).
VITE_GOOGLE_OAUTH_CLIENT_ID=""

# AI (Claude) — root-cause suggestions (Audit module, Breakdowns "what
# happened" assistant) and meaning-aware translation go through the
# `claudeJson` Cloud Function; the Anthropic key lives server-side in Secret
# Manager as ANTHROPIC_API_KEY, never in this file. Set to 1 to skip AI and
# always use the keyword-based root-cause engine / Google's free translate
# endpoint (e.g. local dev without deployed functions).
VITE_DISABLE_AI=""
